#include <stdio.h>
/**
 * main- Entry
 * Cescription: Print all digits of base 10 starting from 0
 * Return: 0
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf(num);
	printf("\n");
	return (0);
}
