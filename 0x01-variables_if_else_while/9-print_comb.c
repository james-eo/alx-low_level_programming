#include <stdio.h>
/**
 * main - Entry
 * Decription: Print possible combination of digits
 * Return: 0
 */
int main(void)
{
	int my_num;

	for (my_num = 0; my_num < 10; my_num++)
		if (num != 10)
		{
			putchar("%d", my_num);
			putchar(',');
			putchar(" ");
		}
	putchar("\n");
	return (0);
}
