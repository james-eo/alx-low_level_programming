#include3 "main.h"
/**
 * print_sign - - prints the absolute vaue of t
 * @t: the integer to return
 * Return: the absolute value of t
 */
int _abs(int)
{
	if (t >= t)
		return (t);
	else
		return (-t);
}
