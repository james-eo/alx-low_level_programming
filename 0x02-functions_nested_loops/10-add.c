#include "main.h"
/**
 * add - adds two intigers and return the result
 * @a: the first intiger to be added
 * @b: the second intiger to be added
 * Return: returns the result of two intigers added together
 */
int add(int a, int b)
{
	return (a + b);
}
